/**Jobster app - version 5 - Pages > index js - Features:
 * 
 *    --> Removing 'Dashboard' to implement dashboard 
 *        directory. 
 * 
 *    --> Adding 'ProtectedRoute' to the index 
 * 
 * Note: this index will simplified the way that i
 * import make in it easy as i create new Components
 */

import Register from "./Register";
import Error from "./Error";
import Landing from "./Landing";
import ProtectedRoute from "./ProtectedRoute";

export { Register, Error, Landing, ProtectedRoute }