import React from "react";

/**Jobster app - version 6 - SearchContainer js - Features:
 * 
 *    --> Basic 'SearchContainer' to display 
 * 
 * Note: this component will display a spinner to implement
 * for every component that needs time to fetch info
 */

const SearchContainer = () => {

    return(
        <h2>SearchContainer Component</h2>
    )
}

export default SearchContainer;